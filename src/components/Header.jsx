import React from 'react'
import items from '../data.json'
import { useContext } from 'react';
import { AppContext } from '../App';

function Header() {
	return (
		<h1>Choose your design of layout</h1>
	)
}

export default Header